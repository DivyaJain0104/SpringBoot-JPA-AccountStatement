
  package com.springboot.springSecurityConfig;
 
	/*
	 * import
	 * org.springframework.security.config.annotation.web.builders.HttpSecurity;
	 * import org.springframework.security.config.annotation.web.configuration.
	 * EnableWebSecurity; import
	 * org.springframework.security.config.annotation.web.configuration.
	 * WebSecurityConfigurerAdapter;
	 * 
	 * @EnableWebSecurity public class AppSecurityConfig extends
	 * WebSecurityConfigurerAdapter{
	 * 
	 * @Override protected void configure(HttpSecurity http) throws Exception {
	 * 
	 * http .authorizeRequests() .antMatchers("/user","/accounts").permitAll()
	 * .antMatchers("/admin/").hasAuthority("admin") .and() .formLogin() .and()
	 * .httpBasic();
	 * 
	 * http.headers().frameOptions().disable(); } }
	 */